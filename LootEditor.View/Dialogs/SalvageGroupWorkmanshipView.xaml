<UserControl x:Class="LootEditor.View.Dialogs.SalvageGroupWorkmanshipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LootEditor.View.Dialogs"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="Workmanship:" />
        <DockPanel Grid.Column="1">
            <xctk:DoubleUpDown DockPanel.Dock="Left" Width="75" Margin="0,0,5,0" Minimum="1" Maximum="10" Increment="0.01" Value="{Binding Workmanship}" />
            <Slider Minimum="1" Maximum="10" TickFrequency="0.01" IsSnapToTickEnabled="True" Value="{Binding Workmanship}" Width="100" />
        </DockPanel>
    </Grid>
</UserControl>
